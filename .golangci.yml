linters:
  disable-all: false
  disable:
    - gochecknoglobals  # we use these here
    - exhaustivestruct  # too strict
    - nlreturn  # too strict for error returns
    - paralleltest  # not needed
    - testpackage  # not needed
    - wrapcheck  # not needed
    - godox  # we still have a lot of TODOs left
    - funlen # strange counting for main()
    - varnamelen # we use these here
    - nonamedreturns
    - exhaustruct
    - scopelint
    - golint
    - interfacer # deprecated since v1.38.0
    - ifshort # v1.48.0
    - musttag
  enable:
    - exportloopref
    - revive
  presets:
    - bugs
    - unused
    - style
  fast: false
